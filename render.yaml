services:
  - type: web
    name: ler-streamlit
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: streamlit run app.py --server.port $PORT --server.enableCORS false --server.address 0.0.0.0
    envVars:
      - key: DATABASE_URL
        sync: false
